<% content_for :content do %>
	<%= turbo_frame_tag "reports_main" do %>
		<div class="page-header page-header-bg">
			<div class="container">
				<div class="row">
					<div class="col-lg-12">
						<div class="bradcam_text text-center">
							<h3>Report Pet</h3>
						</div>
					</div>
				</div>
			</div>
		</div>
		<div class="container mt-5">
			<%= render 'flash' %>
			<%= yield %>
		</div>
	<% end %>
<% end %>
<%= render template: "layouts/application" %>
