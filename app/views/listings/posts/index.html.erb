<%# turbo_frame_tag "posts" do %>
	<div class="container text-center" id="lost_found_main">
		<%= render 'search' %>
		<%= turbo_frame_tag "listing_search_results" do %>
			<%= render 'list', listings: @listings_posts %>
			<div class="row-cols-12 my-auto py-3">
				<div class="col-auto text-center">
					<%== pagy_bootstrap_nav(@pagy) %>
				</div>
			</div>
		<% end %>
	</div>
	<%# end %>
