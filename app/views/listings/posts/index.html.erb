<%= turbo_frame_tag "posts" do %>
  <div class="container text-center">
    <div class="row" id="search">
    </div>
    <div class="row d-flex justify-content-center" id="posts-container">
      <% @listings_posts.each do |listing| %>
        <%= render 'post', listing: listing %>
      <% end %>
    </div>
  </div>
<% end %>
